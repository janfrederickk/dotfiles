# dotfiles

To use these dotfiles you need to install chezmoi. For more information see here https://www.chezmoi.io/install/

```
sudo apt install python3-pygments
sudo apt install highlight

```

Alias in zshrc
`alias cats='highlight -O ansi --force'`
